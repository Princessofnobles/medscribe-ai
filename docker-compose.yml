services:
  medscribe:
    build: .
    container_name: medscribe-ai
    ports:
      - "8080:8080"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - ./audio:/app/audio
      - ./outputs:/app/outputs
    restart: unless-stopped
